apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudflare-secret-sa
automountServiceAccountToken: false
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: cf-ingress-secret-store
spec:
  provider:
    vault:
      auth:
        kubernetes:
          mountPath: kubernetes
          role: cloudflare
          serviceAccountRef:
            name: cloudflare-secret-sa
            audiences:
              - vault
      path: platform
      server: https://vault.kbntx.com
      version: v2
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cf-ingress-secret
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: cf-ingress-secret-store
    kind: SecretStore
  target:
    name: cf-ingress-secret
  dataFrom:
    - extract:
        key: cloudflare
