# Applications overview

Application delivery is split by folder ownership:

- `apps/portfolio` (frontend + chart)
- `apps/homepage` (helm chart)
- `docs/` (documentation site + chart)
- `platform/appsmith` (deployed via platform path)

Use the pages in this section for quick ownership and run commands.
