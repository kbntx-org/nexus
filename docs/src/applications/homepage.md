# Homepage

Homepage is managed primarily as a Helm release in `apps/homepage`.

## What to edit

- Kubernetes objects in `apps/homepage/templates`
- Default values in `apps/homepage/values.yaml`

## Notes

Treat this app as deployment-first: chart correctness matters more than local runtime workflows.
